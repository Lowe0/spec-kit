at some point, we'll need the ability to generate documentation for features implemented using the kit

this should be a module that attaches to the main workflow, not part of the main kit, but not wholly separate either
