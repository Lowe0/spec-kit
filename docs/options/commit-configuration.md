 the choice of how to commit should be configurable 

 if the human wants to author every commit, they can

 if not, a separate commit sub-agent can verify that its predecessor fulfilled its rules, commit, and hand-off to the next TDD step

ideally, the human should be able to start working with agents with manual commits, and once trust is established, move to automatic commits with manual pull request review